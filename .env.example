# Synapse Memory Lab requires no environment variables.
#
# It is a fully client-side static Vite application: no API keys, analytics,
# backend URLs, datasets, or model/checkpoint paths are used.
# Do not add secrets to VITE_* variables; Vite exposes those values to clients.
